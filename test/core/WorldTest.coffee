chai = require 'chai'
chai.should()

# imports
World = require '../../src/core/World'

# scope container
$ = {}

describe 'World', ->

    describe '#addSystem', ->
    describe '#dispose', ->
    describe '#shutdown', ->
    describe '#startup', ->
